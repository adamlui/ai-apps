name: Sync chatgpt-heartbeat/ to adamlui/userscripts/chatgpt/chatgpt-heartbeat/

on:
  push:
    branches: [ master, main ]
    paths:
      - 'chatgpt-heartbeat/**'

jobs:
  build:
    if: github.event.commits[0].committer.username != 'auto-sync-bot'
    runs-on: ubuntu-latest
    steps:

      - name: Checkout adamlui/chatgpt-addons
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.REPO_SYNC_PAT }}
          repository: adamlui/chatgpt-addons
          path: adamlui/chatgpt-addons

      - name: Checkout adamlui/userscripts
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.REPO_SYNC_PAT }}
          repository: adamlui/userscripts
          path: adamlui/userscripts

      - name: Sync chatgpt-heartbeat/ to adamlui/userscripts/chatgpt/chatgpt-heartbeat/
        run: rsync -avh --delete ${{ github.workspace }}/adamlui/chatgpt-addons/chatgpt-heartbeat/ ${{ github.workspace }}/adamlui/userscripts/chatgpt/chatgpt-heartbeat/

      - name: Push to adamlui/chatgpt-addons
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          push_options: '--force'
          add_options: '--all'
          commit_user_email: auto-sync@chatgptevo.com
          commit_message: '${{ github.event.head_commit.message }} ↞ [auto-sync from `adamlui/chatgpt-addons`]'
          file_pattern: 'chatgpt-heartbeat/*'
          repository: adamlui/chatgpt-addons

      - name: Push to adamlui/userscripts
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          push_options: '--force'
          add_options: '--all'
          commit_user_email: auto-sync@chatgptevo.com
          commit_message: '${{ github.event.head_commit.message }} ↞ [auto-sync from `adamlui/chatgpt-addons`]'
          file_pattern: 'chatgpt/chatgpt-heartbeat/*'
          repository: adamlui/userscripts